# Suppress cleanup warnings on Windows
audit-level=moderate
fund=false
# Handle Windows file permission issues
# Note: unsafe-perm is deprecated, using omit instead
# IMPORTANT: Do not omit optional dependencies.
# Rollup/Vite rely on optional platform-specific packages (e.g. @rollup/rollup-linux-x64-gnu)
# for builds in CI and Linux environments.
# If you need to omit something, prefer omitting dev deps instead (omit=dev) in production installs.
# omit=optional
